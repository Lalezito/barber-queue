const Button = ({ onClick, children, className, style }) => {
  return (
    <button
      onClick={onClick}
      className={`btn ${className}`} // Asegúrate de que la sintaxis JSX esté correcta
      style={style}
    >
      {children}
    </button>
  );
};

export default Button;